name: cd
on:
  release:
    types: [published]
permissions:
  contents: read
jobs:
  upload-binaries:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            include: jj-gh.1
          - target: aarch64-apple-darwin
            os: macos-15
            include: jj-gh.1
          - target: x86_64-apple-darwin
            os: macos-15-intel
            include: jj-gh.1
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            include: ""
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - if: matrix.include != ''
        run: cargo run --locked --release --package gen-manpage -- > jj-gh.1
      - uses: taiki-e/upload-rust-binary-action@v1
        with:
          bin: jj-gh
          package: jj-gh
          target: ${{ matrix.target }}
          locked: true
          checksum: sha256
          include: ${{ matrix.include }}
