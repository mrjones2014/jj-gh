id: prefer-turbofish-over-let-type-ascription
snapshots:
  ? |
    let (left, right): (Vec<String>, Vec<String>) = items.unzip();
  : labels:
      - source: 'let (left, right): (Vec<String>, Vec<String>) = items.unzip();'
        style: primary
        start: 0
        end: 62
  ? |
    let mut values: Vec<String> = Vec::new();
  : labels:
      - source: 'let mut values: Vec<String> = Vec::new();'
        style: primary
        start: 0
        end: 41
  ? |
    let values: Vec<String> = items.collect();
  : labels:
      - source: 'let values: Vec<String> = items.collect();'
        style: primary
        start: 0
        end: 42
